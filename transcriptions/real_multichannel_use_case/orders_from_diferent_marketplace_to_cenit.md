The same approach is used for the other Stores.

Each particular Order definition is persisted in Cenit

Notice that each store has a different definition of order, with a different schema in Cenit IO.
